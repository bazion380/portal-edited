{
  "name": "api",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "tsc --noEmit",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "@neondatabase/serverless": "^0.10.0",
    "drizzle-orm": "^0.38.4"
  },
  "devDependencies": {
    "wrangler": "^3.80.0",
    "typescript": "~5.8.2"
  }
}
